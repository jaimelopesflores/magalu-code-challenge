version: "3"
services:
  service:
    build: ./service
    ports:
      - "8000:8000"
    depends_on:
      - mongo
    env_file:
      - ./service/env_files/.env.production
  mongo:
    image: mongo
